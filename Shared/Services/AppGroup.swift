import Foundation

/// Identifier of the shared app group through which the app, the watch app,
/// and the widget extensions exchange state.
enum AppGroup {
    static let id = "group.plastickarma.lead-track"
}
